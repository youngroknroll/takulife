[pytest]
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = tests.py test_*.py *_tests.py
markers =
    unit: pure functions, parsing, value rules — no DB or HTTP.
    domain: model/service business behavior and invariants — DB as needed.
    web: HTTP request/response, auth, permission, and error translation via the Django/DRF test client.
    contract: architecture, settings, migration, and performance contracts.
    slow: security lockouts, real files, and abnormal-recovery scenarios — explicit opt-in.
    e2e: end-to-end journey tests (Playwright + Django live_server). Excluded by default; run with -m e2e tests/e2e.
# The 2026-07-22 "backend logic only" policy was partially withdrawn on
# 2026-09-09: journey-level e2e tests are back in, but excluded from the
# default run. A temporary "-m <expr>" run replaces addopts' "-m", so write
# "-m \"<expr> and not e2e\"" to keep e2e out unless you mean to run it.
addopts = -m "not e2e" --strict-markers
